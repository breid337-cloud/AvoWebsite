node_modules/
*.log
.DS_Store
.env
.env.*

# Per-client working data. Keep the folder, ignore the contents — except the
# brannigan worked example, committed as documentation, and avosolution, which
# is our own site rather than a client's private data.
clients/*
!clients/.gitkeep
!clients/brannigan/
clients/brannigan/dist/
!clients/avosolution/
clients/avosolution/dist/

# Build output
dist/
*.zip
*.tmp.mjs

# Deploy credentials
avo.secrets.json
