node_modules/
*.log
.DS_Store
.env
.env.*

# Per-client working data. Keep the folder, ignore the contents — except the
# brannigan worked example, committed as documentation.
clients/*
!clients/.gitkeep
!clients/brannigan/
clients/brannigan/dist/

# Build output
dist/
*.zip
*.tmp.mjs

# Deploy credentials
avo.secrets.json

# Netlify CLI working directory (link state and generated build config)
.netlify/
